# NumPy Revision

This folder contains the complete revision material for the NumPy module.

## Revision Scope

The revision covers NumPy Days 1–6.

### Day 1 — NumPy Fundamentals

- Importing NumPy
- Creating arrays
- `np.array()`
- NumPy `ndarray`
- Indexing
- Negative indexing
- `len()`
- `np.mean()`
- `np.max()`
- `np.min()`
- `np.sum()`
- `np.sort()`
- Boolean filtering
- Vectorized operations

### Day 2 — Array Operations

- One-dimensional arrays
- Two-dimensional arrays
- `shape`
- `ndim`
- Row selection
- Column selection
- Array slicing
- Reshaping
- `reshape()`
- Modifying array values
- Vectorized arithmetic
- Broadcasting
- `np.zeros()`
- `np.ones()`
- `np.arange()`

### Day 3 — Array Operations and Data Manipulation

- Array dimensions
- 2D indexing
- Row and column operations
- Slicing
- Reshaping
- Vectorized operations
- Broadcasting
- Array creation

### Day 4 — Array Aggregation and Statistical Operations

- Aggregation
- `axis`
- `np.sum()`
- `np.mean()`
- `np.max()`
- `np.min()`
- `np.std()`
- `np.median()`
- `np.argmax()`
- `np.argmin()`
- `np.ptp()`
- `np.any()`
- `np.all()`
- Boolean counting
- Combined Boolean conditions
- `axis=0`
- `axis=1`

### Day 5 — Array Manipulation and Data Cleaning

- `np.concatenate()`
- `np.vstack()`
- `np.hstack()`
- `np.append()`
- `np.insert()`
- `np.delete()`
- `np.where()`
- Conditional replacement
- `np.nan`
- `np.isnan()`
- `np.nanmean()`
- `np.nanmin()`
- `np.nanmax()`
- Missing-value detection
- Missing-value replacement
- Missing-value removal
- Data cleaning
- Column-based imputation

### Day 6 — Random Numbers and Data Generation

- `np.random.rand()`
- `np.random.randint()`
- `np.random.uniform()`
- `np.random.normal()`
- `np.random.choice()`
- `np.random.seed()`
- Reproducible randomness
- Synthetic datasets
- Random student data
- Statistical analysis of generated data

---

## Core Concepts to Master

### Arrays

Understand how NumPy arrays differ from normal Python lists.

### Indexing and Slicing

Be able to access:

- individual values
- rows
- columns
- ranges of values

### Shape and Dimensions

Understand:

```python
array.shape
array.ndim